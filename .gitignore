# ==========================================================================
#  KunxunAuth —— 仓库忽略规则
# ==========================================================================

# ---------- Java / Maven ----------
target/
*.class
*.log

# ---------- Gradle ----------
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# ---------- Minecraft 构建缓存 ----------
# 这些目录里是 Mojang 的官方客户端 jar、反混淆映射、Forge/NeoForge 的
# userdev 产物。体积以 GB 计，而且重新分发 Minecraft 二进制违反 Mojang EULA，
# 绝对不能提交。
**/loom-cache/
**/mavenizer/repo/
**/mavenizer/dependencies/
**/moddev/artifacts/
**/tmp/neoformruntime/
**/tmp/createMinecraftArtifacts/

# ---------- 构建产物中的中间件 ----------
client-mod/dist/_backup/
client-mod/dist/*.zip

# ---------- 本地运行目录与设备凭据 ----------
run/
kunxun-device.properties
secret.local.yml
*.mca
*.mcr

# ---------- 运行时数据（开源仓库里绝对不能出现）----------
# plugins/ 是服务器上的插件目录：里面有真实的 secret.yml（邮箱授权码）、
# accounts.db（玩家密码哈希）以及服务器自己生成的 device-server-id.txt。
# 整份忽略，免得哪天把服务器目录拷进工作区再一起提交上去。
plugins/
# 账号库：含 PBKDF2 密码哈希与玩家邮箱
**/accounts.db
**/accounts.db-*
**/accounts.db.*
# 服务器设备标识：参与设备签名，泄露就等于给了跨服重放挑战的机会
**/device-server-id.txt
# 真实凭据文件；仓库里只保留 src/main/resources/secret.yml 这个空模板
**/secret.yml
!src/main/resources/secret.yml
# 玩家/白名单等运行期数据
**/usercache.json
**/whitelist.json

# ---------- 密钥 / 证书 ----------
*.pem
*.p12
*.jks
*.keystore

# ---------- IDE ----------
.idea/
*.iml
*.ipr
*.iws
.vscode/

# ---------- 系统 ----------
Thumbs.db
Desktop.ini
.DS_Store

# ---------- 工具产物 ----------
.workbuddy/
.trae-html-share-packages/
